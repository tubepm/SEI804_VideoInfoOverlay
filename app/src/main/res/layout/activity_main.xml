<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/overlayTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:padding="12dp"
        android:text="@string/starting" />

    <TextView
    android:id="@+id/overlayTextView2"
    android:layout_width="0dp" <!-- 关键修改：宽度自适应 -->
    android:layout_height="wrap_content"
    android:layout_alignParentTop="true"
    android:layout_toStartOf="@id/overlayTextView"
    android:layout_alignParentStart="true" <!-- 锚定左侧边界 -->
    android:layout_marginEnd="8dp" <!-- 与主文本间隔 -->
    android:padding="12dp"
    android:background="#33000000" <!-- 添加轻量背景 -->
    android:text="状态监控"
    android:maxLines="1"
    android:ellipsize="end"
    android:textColor="#FFF"/>

</RelativeLayout>
